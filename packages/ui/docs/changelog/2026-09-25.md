# 2026-09-25

- [feat] InstallCommand: caixa monoespaçada que copia o comando de instalação (clique na caixa com o mouse; `<code>` focável e `button` como acionador de teclado; feedback "Copiado!" por 2 s, região `role="status"`, fallback de seleção)
- [feat] InstallCommandTabs: abas de comandos sobre o `Tabs`, aba persistida ao trocar, nota de telemetria opcional
- [feat] useStoredValue: hook de valor em `localStorage` sincronizado entre instâncias e abas, snapshot de servidor `null`
- [chore] infra de testes: Vitest + Testing Library (jsdom) em `packages/ui`, scripts `test` e `typecheck`, task `test` no `turbo.json`
- [fix] InstallCommand: separa o scroll do botão (`scrollable-region-focusable`, WCAG 2.1.1); root vira `div` e o ref passa a ser `HTMLDivElement`
- [chore] `lint` (eslint + typescript-eslint + react-hooks) em `packages/ui`; Vitest com `globals: true`, sem stub de `jest`
- [docs] useStoredValue: documentada a prioridade localStorage > memória
