# Changelog — 2026-06-03

## packages/ui — Header Compound Components

Implementação completa do sistema de Header compartilhado entre todos os apps do monorepo.

### Novos componentes
- `Header` — root compound, `scrollAware` (transparente→frosted no scroll) e `borderColor` props
- `Header.Logo` — link de identidade do app
- `Header.Nav` — nav desktop (hidden mobile), aceita `className` para override de breakpoint
- `Header.Actions` — slot de ações à direita (ThemeToggle, LocaleSwitcher, social links)
- `Header.Menu` — side drawer móvel: híbrido controlled/uncontrolled, `side="left|right"`, body scroll lock, close-on-link-click, portal em `document.body` (escapa stacking context de `backdrop-filter`)
- `NavLink` — `href`, `external`, `active` props; tokens canônicos
- `ThemeToggle` — SSR-safe, `.dark` class toggle, `localStorage` persistência

### Fix crítico de UI
`Header.Menu` drawer renderizado via `createPortal(drawer, document.body)` — evita que `backdrop-filter: blur()` no header crie stacking context que coloca o drawer sob o conteúdo da página.

---

## Migração de apps

### apps/tools
- `SiteHeader.tsx` migrado para `Header` compound
- `theme-toggle.tsx` local deletado (duplicata de `@nico.dev/ui`)
- URLs via `NEXT_PUBLIC_*` envs com fallbacks

### apps/challenges
- `SiteHeader.tsx` criado com `Header scrollAware`, social links, nav via envs
- Header wired no `layout.tsx`

### apps/blog
- `Header.astro` e `ThemeToggle.tsx` locais deletados
- `BlogHeader.tsx` como React island — hamburguer direto (dispara `toggle-sidebar`), sem `Header.Menu` (evita conflito com `CategorySidebar`)
- `global.css` migrado: `@nico.dev/ui/tokens.css`, paleta canônica, `--accent`/`--ring` tokens adicionados, `@source` para packages/ui
- `astro.config.mjs`: `vite.define` expõe `NEXT_PUBLIC_*` vars aos React islands
- `config.ts`: exporta `SITES` com URLs/labels das envs
- Anti-FOUC: dark-default mantido via `class="dark"` no `<html>`

### apps/web-nico.dev.br
- `Navbar.tsx` deletado
- `SiteHeader.tsx` criado: `Header scrollAware`, `Header.Menu side="right"`, nav i18n, LocaleSwitcher, ThemeToggle
- `globals.css`: aliases canônicos (`--foreground`, `--accent`, `--border`, `--ring`) + `@source` para packages/ui
- `projects.ts`: `demoUrl` do metrônomo via `NEXT_PUBLIC_METRONOME_URL`

---

## Infraestrutura

### Envs centralizadas
- Root `.env` + `.env.example` com `NEXT_PUBLIC_*` para Portfolio, Challenges, Blog, Tools, Metronome
- `turbo.json` com `globalEnv` declarado

### Segurança — Dependabot
- Next.js 16.2.1 → **16.2.6** em todos os apps (16 high + 9 medium CVEs: middleware bypass, DoS, cache poisoning, SSRF, XSS)
- `pnpm.overrides`: `postcss >= 8.5.10` + `next>postcss >= 8.5.10` força 8.5.14 (PostCSS XSS CVE)

### Storybook
- Stories: `Header` (5 variações), `NavLink`, `ThemeToggle`
- `preview-body.html`: reset `min-height` em html/body para melhor visualização

---

## Commits desta sessão

- `cf8b690` fix(ui): portal Header.Menu drawer to escape backdrop-filter stacking context
- `d366ea7` fix(deps): upgrade next 16.2.1→16.2.6, override postcss to >=8.5.10
- `80d4882` feat(ui): add Header compound components to packages/ui
